// Client entry point: importing a component module runs its @customElement
// registration as a side effect, defining <soundmate-app> in the browser.
import './elements/soundmate-app/soundmate-app';
