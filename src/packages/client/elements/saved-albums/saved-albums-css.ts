// <saved-albums> has no styling beyond the shared top-list base. Re-export the
// common styles so the element keeps the standard
// `import { styles } from './<name>-css'`.
export { topListStyles as styles } from '../common/top-list-css';
